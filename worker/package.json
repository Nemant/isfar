{
  "name": "rihla-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker serving GET /api/flight for the Rihla app (AeroDataBox -> engine record).",
  "main": "src/index.js",
  "scripts": {
    "test": "node --test \"test/*.mjs\"",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  }
}
